from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='it's ITI project that we develop a data-driven solution for IT institue to understand relationships between the jobs and the technologies in market',
    author='0xnrous',
    license='MIT',
)
